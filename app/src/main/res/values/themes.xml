<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Buy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Buy" parent="Base.Theme.Buy">
        <item name="android:statusBarColor">@color/blue_400</item>
        <item name="android:navigationBarColor">@color/blue_400</item>
    </style>
</resources>